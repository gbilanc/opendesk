# Legacy distribution scripts

These scripts were used by the old distribution system (PyInstaller + AppImage/DMG/NSIS).
They are kept for reference only.

**New distribution method:** `pip install opendesk`

See [`scripts/bootstrap.sh`](../bootstrap.sh) or [`scripts/bootstrap.ps1`](../bootstrap.ps1)
for the current installation method.
